import { NextComponentType } from "next";
import Style from "@/scss/background.module.scss"

const BackgroundForm: NextComponentType = () => {
    return <div className={Style.Background}/>
}

export default BackgroundForm