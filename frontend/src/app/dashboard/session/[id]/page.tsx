import { NextPage } from "next"

interface Props {
    params: {
        id: number
    }
}

const page: NextPage<Props> = ({params}) => {
    return(
        <>
        </>
    )
}

export default page