import { NextPage } from "next"

interface Props {
    params: {
        id: number
    }
}

const page: NextPage<Props> = ({params}) => {
    return(
        <div>
            
        </div>
    )
}

export default page